name: CI Pipeline

on:
  push:
    branches: [ develop, main, feature/* ]

  pull_request:
    branches: [ develop, main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: CI check
        run: echo "CI pipeline running OK"

      - name: Validate static site
        run: echo "Static HTML/CSS project — no build step"
